Get Company Name from given mac address using api.macaddress.io REST api

./mac_api.py --help
usage: MAC address lookup [-h] --macaddr MACADDR --apitoken APITOKEN

optional arguments:
  -h, --help           show this help message and exit
  --macaddr MACADDR    MAC address
  --apitoken APITOKEN  api.macaddress.io API token
